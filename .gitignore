node_modules/
.expo/
dist/
android/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.env.local
.env.*.local

# Firebase config (do not commit — contains project keys)
google-services.json
GoogleService-Info.plist

# Google Play Console service account key (do not commit — grants
# publishing access to the Play Store listing)
play-service-account.json

# Firebase CLI local cache (per-machine deploy state, not source)
.firebase/

# macOS Finder metadata
.DS_Store

# Claude / agent workspace (per-machine session state)
.agents/

# Skill lock files (per-machine cache, regenerated on demand)
skills-lock.json


# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli.claude/worktrees/
