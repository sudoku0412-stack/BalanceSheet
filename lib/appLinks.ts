/** Live on the App Store; Android isn't published yet (still Play
 *  Console closed testing per HANDOVER.md), so there's no Play Store
 *  URL to link to. Invite text below includes this unconditionally —
 *  an iOS recipient can tap straight through, and an Android recipient
 *  just sees a link that doesn't apply to them yet alongside the
 *  install instructions, same as before this URL existed. Update this
 *  (and add a Play Store URL alongside it) once Android goes live. */
export const APP_STORE_URL = 'https://apps.apple.com/ca/app/nestexpensetracker/id6797353891';
